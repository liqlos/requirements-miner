# mini-todo

Tiny CLI todo app used as a requirements-miner test fixture.

## Usage

```bash
python3 src/todo.py add "buy milk"
python3 src/todo.py list
python3 src/todo.py done 1
```

Tasks are stored in `todo.json` in the working directory.
